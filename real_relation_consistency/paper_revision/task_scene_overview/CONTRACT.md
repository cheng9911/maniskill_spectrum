# Figure contract
Python nature-figure workflow. IEEE two-column descriptive scene figure,180mm width. No numerical response curves. A explains actual keyed/circular geometry using actual simulation replay closeups plus explicit schematic sectional views; B shows four replayed states of one successful keyed rollout; C shows four representative state-level relation scenes, not four additional closed-loop robot executions. Every source frame/crop/time is saved. Source old isolated-peg socket render rejected because it creates display geometry not exact environment. Replayed states are not new simulation success trials. Schematics reflect nominal geometry dimensions but yaw-free statement is permissibility, not learned alpha=0. Existing figure files preserved.
